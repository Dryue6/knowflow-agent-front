[2026-05-26 11:04:43] 文档上传后索引进度与资料层级交互优化
- 修改内容：文档管理页增加索引中文档的轻量轮询和任务进度展示，轮询刷新不再触发整表 loading；资料层级修改改为索引中也可直接操作，并通过乐观更新提升响应速度，失败时自动回滚；mock server 同步模拟上传后异步索引完成流程，保证 mock 模式下交互不失真。
- 注意事项：前端仍保留 MinIO 直传流程，索引进度依赖后端 `/documents/{id}/index-job` 返回最新任务；如果后端任务状态为 CANCELLED，前端类型已兼容但当前列表主要通过文档状态决定是否持续轮询。

[2026-05-26 12:00:26] 简化上传中的前端状态展示
- 修改内容：移除文档管理页对索引任务的自动轮询和进度条展示，上传时仅保留“正在上传...”状态，上传完成后刷新一次文档列表；资料层级的索引前可编辑和失败回滚逻辑保持不变。
- 注意事项：后端仍会维护索引任务进度，前端当前不主动展示或轮询该进度；如需查看索引状态，可通过刷新列表观察文档状态变化。
